export enum WalletUpdateType {
  ADD = "ADD",
  SUBTRACT = "SUBTRACT",
}

export enum ReservationStatus {
  INCOMING = 1,
  CONFIRMED = 2,
  REJECTED = 3,
  DELETED = 4,
}

export enum UserRole {
  ADMIN = 2,
  USER = 1,
}
